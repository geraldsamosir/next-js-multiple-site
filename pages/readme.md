# make page directory
page directory in here is same as subdomain
so please use kebabcase to make naming
example:
```
  ada
  ada-satu
  admin
  admin-panel
```

or use `.` to split

example: 
```
  test.admin
  test.ok
```